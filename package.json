{
  "name": "sw-builder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/service-worker.ts --bundle --outfile=dist/service-worker.js --platform=node --format=cjs --sourcemap"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
