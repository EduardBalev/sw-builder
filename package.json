{
  "name": "sw-builder",
  "version": "1.0.0",
  "description": "A CLI tool to build service workers based on user configuration",
  "main": "index.js",
  "bin": {
    "sw-builder": "./cli.js"
  },
  "scripts": {
    "build": "node cli.js --config=sw-config.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@simple_js/log-styler": "^1.0.1",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
