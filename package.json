{
  "name": "sw-builder",
  "version": "1.0.0",
  "description": "A CLI tool to build service workers based on user configuration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "sw-builder": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "serve:demo": "http-server demo -p 3000",
    "build:demo": "ts-node ./src/index.ts --config=./demo/demo-config.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^4.0.1",
    "esbuild": "^0.24.0",
    "http-server": "^14.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}